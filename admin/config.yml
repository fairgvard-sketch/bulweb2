backend:
  name: git-gateway
  branch: main

media_folder: img
public_folder: img

collections:
  - name: "menu"
    label: "Menu"
    files:
      - name: "coffee"
        label: "Coffee"
        file: "menu.html"
        fields:
          - { label: "Page content", name: "body", widget: "markdown" }
